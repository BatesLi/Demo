<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.dankal.demo">

  <uses-permission android:name="android.permission.INTERNET"/>

  <!-- To auto-complete the email text field in the login form with the user's emails -->
  <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
  <uses-permission android:name="android.permission.READ_PROFILE"/>
  <uses-permission android:name="android.permission.READ_CONTACTS"/>

  <application
      android:name="cn.dankal.basic_lib.DankalApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".user.LoginActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".HandlerActivity">
    </activity>
    <activity android:name=".BubbleSortActivity">
    </activity>
    <activity android:name=".ViewPagerHeaderMvp.activity.StartActivity">
    </activity>
    <activity android:name=".ViewPagerHeaderMvp.activity.LeafActivity">
    </activity>
    <activity android:name=".interfaceTest.InterfaceTestActivity">
    </activity>
    <activity android:name=".KotlinOne.KotlinOneActivity">
    </activity>
  </application>

</manifest>